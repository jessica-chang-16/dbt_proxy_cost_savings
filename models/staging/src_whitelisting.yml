version: 2

sources:
  - name: google_sheets_raw_clients
    description: >
      raw data file containing the retailer information and whether it is whitelisted or not.
    database: cost-savings-tl
    schema: analytics
    tables:
      - name: raw_client_data